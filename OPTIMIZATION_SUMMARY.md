# 交互体验与稳定性优化摘要

日期：2026-07-15

## 本次已落地

1. **Git / 工作区止血**
   - 清空错误暂存删除（计划文档、主题产物）
   - 恢复误删的 `SECURITY_AUDIT.md`
   - 回退破坏流程图测试的全局连线评分调参
   - 将损坏对象诊断输出备份到 `.repair-backup/`

2. **流程图缩放**
   - 按钮缩放按级别自适应步长（小级别更细、大级别更快）
   - 滚轮缩放同步动态步长

3. **思维导图连线**
   - 近水平节点直线段连接，去掉无意义零高度折角
   - 不改动曲线控制点公共 helper，避免布局副作用

4. **命令面板 / 搜索**
   - 完善命令面板键盘导航（↑↓ 选择、Enter 执行、Esc 关闭、Ctrl/Cmd+K 开关）
   - 搜索结果上下键切换后同步当前索引与计数
   - 搜索结果列表更新时统一同步活动项状态

5. **自动保存节奏**
   - 流程图自动保存间隔：`60s -> 15s`（仍防抖，不每帧落盘）
   - 流程图恢复草稿写入防抖：`500ms -> 800ms`，降低拖拽期间 IO

## 明确没有做的高风险改动

- 未继续对 `scoreFlowchartConnectionCandidate` 做“少转角优先”的激进全局权重改写  
  该改写已验证会破坏方向合法性、正交约束与导出路径契约测试。

## 验证

- `node --test tests/flowchart-document.test.mjs`：应全绿
- `npm run test:desktop-flow`：覆盖命令面板 / 搜索 / 首页 / 流程图主链路
